interface:
  display_name: "Jira Auto Start"
  short_description: "착수 가능 여부를 판정하고 가능한 Jira 일을 실행합니다"
  default_prompt: "Use $jira-auto-start to report which assigned Jira todo items are startable after prerequisite checks, then execute the first startable item."
policy:
  allow_implicit_invocation: false
