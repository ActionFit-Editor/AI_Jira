interface:
  display_name: "Jira Run"
  short_description: "선택한 Jira 작업을 승인 흐름부터 PR까지 실행합니다"
  default_prompt: "Use $jira-run to implement the selected Jira issue, using a Korean plan approval view when refinement is required."
policy:
  allow_implicit_invocation: false
