interface:
  display_name: "Jira Plan"
  short_description: "구현 결정을 함께 확정하고 Jira 계획을 승인합니다"
  default_prompt: "Use $jira-plan to collaboratively resolve material decisions, close decision discovery, review the complete Korean approval view, and create or refine the issue after approval."
policy:
  allow_implicit_invocation: false
